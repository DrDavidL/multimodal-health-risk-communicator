# Demo application dependencies
# Pin to gradio 4.44.1 (last stable 4.x) to avoid JSON schema bugs in 5.x
# Pin huggingface_hub to 0.25.x which still has HfFolder (removed in 0.26+)
gradio==4.44.1
huggingface_hub>=0.24.0,<0.26.0
Pillow>=9.0.0
torch>=2.0.0
transformers>=4.40.0
peft>=0.10.0
accelerate>=0.27.0
spaces>=0.19.0
